const config = {
  API_URL: 'https://medical-service-backend-eg8t.onrender.com',
  NODE_ENV: process.env.NODE_ENV || 'development',
  PUBLIC_URL: process.env.PUBLIC_URL || '/',
};

export default config; 